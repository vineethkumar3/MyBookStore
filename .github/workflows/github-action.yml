name: Github action pipeline
on:
  push:
    branches: 
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Respository
        run: |
          echo "Hello world"
        
